# Pinned to the locally-verified stack (live API test passes on these exact
# versions) so Streamlit Cloud reproduces it 1:1.
streamlit==1.62.0
openai==3.3.1
python-dotenv==1.2.3
python-docx==1.2.0
pandas==3.0.5
openpyxl==3.1.5
pypdf==6.16.1
requests==2.34.2
